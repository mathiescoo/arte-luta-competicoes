"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
import { Radio, Trophy } from "lucide-react";
type Board={session_name:string;event_name:string;ring_name:string|null;match_status:string|null;phase:string|null;blue_name:string|null;green_name:string|null;blue_votes:number;green_votes:number;winner_name:string|null};
export default function PublicDisplay({board,pin}:{board:Board;pin:string}){const router=useRouter();useEffect(()=>{const timer=window.setInterval(()=>router.refresh(),8000);return()=>window.clearInterval(timer)},[router]);const live=board.match_status==="live";return <main className="public-display"><header><div className="display-logo">AL</div><div><span>ARTE-LUTA BRASIL</span><h1>{board.event_name}</h1></div><div className="display-session"><Radio/> {live?"AO VIVO":"TELÃO"}<small>{board.ring_name||board.session_name}</small></div></header>{board.blue_name&&board.green_name?<section className="display-match"><div className="display-phase">{board.phase||"Confronto"}</div><div className="display-fighters"><article className="blue"><span>COMPETIDOR AZUL</span><strong>{board.blue_name}</strong><b>{board.blue_votes}</b></article><i>×</i><article className="green"><span>COMPETIDOR VERDE</span><strong>{board.green_name}</strong><b>{board.green_votes}</b></article></div>{board.winner_name?<div className="display-winner"><Trophy/> Vencedor: <b>{board.winner_name}</b></div>:<p>{live?"Votos em atualização automática":"Aguardando início do confronto"}</p>}</section>:<section className="display-idle"><Radio/><h2>Aguardando a próxima disputa</h2><p>O telão será atualizado automaticamente assim que um confronto for iniciado.</p></section>}<footer>Sessão {pin} · atualização automática</footer></main>}
