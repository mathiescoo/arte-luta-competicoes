"use client";
import { useMemo, useState } from "react";
import { Copy, MonitorPlay, Plus } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
type Event={id:string;name:string;rings:Array<{id:string;name:string}>};type Session={id:string;name:string;event_id:string;ring_id:string|null;pin:string;active:boolean;created_at:string};
export default function DisplayManager({organizationId,initialEvents,initialSessions}:{organizationId:string;initialEvents:Event[];initialSessions:Session[]}){const [sessions,setSessions]=useState(initialSessions);const [eventId,setEventId]=useState(initialEvents[0]?.id||"");const [ringId,setRingId]=useState("");const [name,setName]=useState("Telão principal");const [message,setMessage]=useState("");const current=useMemo(()=>initialEvents.find((item)=>item.id===eventId),[eventId,initialEvents]);async function create(){if(!eventId)return;const pin=Math.floor(100000+Math.random()*900000).toString();const {data,error}=await createClient().from("display_sessions").insert({organization_id:organizationId,event_id:eventId,ring_id:ringId||null,name,pin,active:true}).select("id,name,event_id,ring_id,pin,active,created_at").single();if(error||!data){setMessage(error?.message||"Não foi possível criar a sessão.");return;}setSessions((items)=>[data as Session,...items]);setMessage("Sessão criada. Use o PIN exibido no telão.")}return <section className="operations-workspace"><div className="display-form"><label>Evento<select value={eventId} onChange={(e)=>{setEventId(e.target.value);setRingId("")}}>{initialEvents.map((event)=><option key={event.id} value={event.id}>{event.name}</option>)}</select></label><label>Roda<select value={ringId} onChange={(e)=>setRingId(e.target.value)}><option value="">Telão geral</option>{current?.rings.map((ring)=><option key={ring.id} value={ring.id}>{ring.name}</option>)}</select></label><label>Nome<input value={name} onChange={(e)=>setName(e.target.value)}/></label><button className="primary" onClick={create} disabled={!eventId}><Plus/>Criar sessão</button></div>{message&&<div className="login-success">{message}</div>}<div className="display-list">{sessions.length?sessions.map((session)=><article key={session.id}><MonitorPlay/><div><strong>{session.name}</strong><span>PIN de acesso: <b>{session.pin}</b></span></div><button onClick={()=>navigator.clipboard.writeText(session.pin)} title="Copiar PIN"><Copy/></button></article>):<div className="empty-state compact"><MonitorPlay/><h2>Nenhum telão criado</h2><p>Crie uma sessão para gerar um PIN de acesso.</p></div>}</div></section>}
