"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { createClient } from "@/lib/supabase/client";
export default function CompleteAuthPage(){const [error,setError]=useState("");useEffect(()=>{let active=true;async function complete(){const url=new URL(window.location.href);const code=url.searchParams.get("code");const next=url.searchParams.get("next")||"/painel";if(!code){if(active)setError("O Google não retornou um código de acesso.");return}const {error:exchangeError}=await createClient().auth.exchangeCodeForSession(code);if(exchangeError){if(active)setError(exchangeError.message);return}window.location.replace(next.startsWith("/")?next:"/painel")}void complete();return()=>{active=false}},[]);if(!error)return null;return <main className="login-page"><section className="login-brand"><div className="brand-mark">AL</div><span>ARTE-LUTA BRASIL</span><h1>Não foi possível<br/>concluir o acesso.</h1></section><section className="login-panel"><div className="login-error"><strong>Detalhe do retorno:</strong><br/>{error}</div><p>Envie esta mensagem caso o problema continue.</p><Link className="primary" href="/entrar">Voltar ao login</Link></section></main>}
