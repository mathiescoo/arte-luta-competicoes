"use client";
import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { KeyRound } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
export default function ResetPasswordPage(){const router=useRouter();const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [error,setError]=useState("");const [ready,setReady]=useState(false);const [loading,setLoading]=useState(false);useEffect(()=>{const supabase=createClient();supabase.auth.getSession().then(({data})=>setReady(Boolean(data.session)));const {data:listener}=supabase.auth.onAuthStateChange((event)=>{if(event==="PASSWORD_RECOVERY"||event==="SIGNED_IN")setReady(true)});return()=>listener.subscription.unsubscribe()},[]);async function update(event:FormEvent){event.preventDefault();if(password.length<8){setError("Use pelo menos 8 caracteres.");return;}if(password!==confirm){setError("As senhas não coincidem.");return;}setLoading(true);const {error:updateError}=await createClient().auth.updateUser({password});if(updateError){setError("O link expirou ou a senha não pôde ser alterada.");setLoading(false);return;}router.push("/painel");router.refresh()}return <main className="login-page"><section className="login-brand"><div className="brand-mark">AL</div><span>RECUPERAÇÃO SEGURA</span><h1>Crie uma nova<br/>senha de acesso.</h1><p>O link é temporário e só pode ser usado pela conta que solicitou a recuperação.</p></section><section className="login-panel"><form onSubmit={update}><div className="login-icon"><KeyRound/></div><span className="eyebrow">NOVA SENHA</span><h2>Redefinir acesso</h2>{ready?<><label>Nova senha<input type="password" required value={password} onChange={(e)=>setPassword(e.target.value)}/></label><label>Confirmar senha<input type="password" required value={confirm} onChange={(e)=>setConfirm(e.target.value)}/></label>{error&&<div className="login-error">{error}</div>}<button className="primary" disabled={loading}>{loading?"Salvando...":"Salvar nova senha"}</button></>:<div className="login-error">Este link não possui uma sessão válida. Solicite um novo link na tela de login.</div>}<Link className="forgot-button" href="/entrar">Voltar ao login</Link></form></section></main>}
