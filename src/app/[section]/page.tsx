import Link from "next/link";
import { redirect, notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
const sections:Record<string,{title:string;description:string;items:string[]}>={participantes:{title:"Participantes",description:"Cadastro, inscrições e importações.",items:["Cadastro manual","Importação CSV","Revisão de duplicidades"]},resultados:{title:"Resultados",description:"Homologação, classificações e pódios.",items:["Resultados por categoria","Homologações","Relatórios"]},rodas:{title:"Controle das rodas",description:"Rodas e confrontos em tempo real.",items:["Status das rodas","Juízes designados","Fila de confrontos"]},telao:{title:"Telões ao vivo",description:"Sessões geral e específicas por roda.",items:["Telão principal","Telões por roda","Código de acesso"]},configuracoes:{title:"Configurações",description:"Identidade, regras e segurança.",items:["Organização","Identidade visual","Segurança"]},ajuda:{title:"Central de ajuda",description:"Orientações para operar seus eventos.",items:["Criar evento","Configurar rodas","Publicar resultados"]}};
export const dynamic="force-dynamic";
export default async function SectionPage({params}:{params:Promise<{section:string}>}){const {section}=await params;const current=sections[section];if(!current)notFound();const supabase=await createClient();const {data}=await supabase.auth.getClaims();if(!data?.claims?.sub)redirect("/entrar");return <main className="management-page"><Link className="back-link" href="/painel">← Voltar para visão geral</Link><div className="management-top"><div><span className="eyebrow">MÓDULO</span><h1>{current.title}</h1><p>{current.description}</p></div></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:14,marginTop:24}}>{current.items.map((item,index)=><article className="form-section" key={item}><span className="eyebrow">0{index+1}</span><h2 style={{marginTop:10}}>{item}</h2><p style={{color:"#77786f",fontSize:12,lineHeight:1.6}}>Este fluxo será habilitado na próxima etapa.</p></article>)}</div></main>}
