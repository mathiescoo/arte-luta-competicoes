"use client";
import { FormEvent, useMemo, useState } from "react";
import { Plus, Search, UserRound, X } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
type EventItem={id:string;name:string;competitions:Array<{id:string;name:string;categories:Array<{id:string;name:string}>}>};
type Participant={id:string;full_name:string;birth_date:string|null;private_data:Record<string,unknown>;registrations:Array<{id:string;event_id:string;category_id:string;attendance_confirmed:boolean;payment_confirmed:boolean}>};
export default function ParticipantManager({organizationId,initialEvents,initialParticipants}:{organizationId:string;initialEvents:EventItem[];initialParticipants:Participant[]}){const [participants,setParticipants]=useState(initialParticipants);const [open,setOpen]=useState(false);const [query,setQuery]=useState("");const [eventId,setEventId]=useState(initialEvents[0]?.id||"");const [error,setError]=useState("");const [loading,setLoading]=useState(false);const event=initialEvents.find((item)=>item.id===eventId);const categories=event?.competitions.flatMap((competition)=>competition.categories.map((category)=>({...category,competition:competition.name})))||[];const filtered=useMemo(()=>participants.filter((item)=>item.full_name.toLowerCase().includes(query.toLowerCase())),[participants,query]);async function submit(ev:FormEvent<HTMLFormElement>){ev.preventDefault();setLoading(true);setError("");const form=new FormData(ev.currentTarget);const categoryId=String(form.get("category_id")||"");if(!eventId||!categoryId){setError("Selecione o evento e a categoria.");setLoading(false);return;}const payload={organization_id:organizationId,full_name:String(form.get("full_name")||""),birth_date:String(form.get("birth_date")||"")||null,private_data:{phone:String(form.get("phone")||""),guardian:String(form.get("guardian")||""),school:String(form.get("school")||""),teacher:String(form.get("teacher")||""),graduation:String(form.get("graduation")||""),sex:String(form.get("sex")||""),notes:String(form.get("notes")||"")}};const supabase=createClient();const {data:participant,error:participantError}=await supabase.from("participants").insert(payload).select("id,full_name,birth_date,private_data").single();if(participantError||!participant){setError(participantError?.message||"Erro ao cadastrar participante.");setLoading(false);return;}const {data:registration,error:registrationError}=await supabase.from("registrations").insert({event_id:eventId,participant_id:participant.id,category_id:categoryId,data:{source:"manual"}}).select("id,event_id,category_id,attendance_confirmed,payment_confirmed").single();if(registrationError||!registration){setError("Participante criado, mas não foi possível concluir a inscrição.");setLoading(false);return;}setParticipants((items)=>[...items,{...participant,registrations:[registration]} as Participant].sort((a,b)=>a.full_name.localeCompare(b.full_name)));setOpen(false);setLoading(false)}return <section className="participant-workspace"><div className="participant-tools"><label><Search/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Buscar participante"/></label><button className="primary" onClick={()=>setOpen(true)}><Plus/>Novo participante</button></div>{filtered.length?<div className="participant-table"><div className="table-head"><span>Participante</span><span>Idade</span><span>Inscrições</span><span>Status</span></div>{filtered.map((participant)=>{const age=participant.birth_date?Math.max(0,new Date().getFullYear()-new Date(participant.birth_date).getUTCFullYear()):null;return <article key={participant.id}><div><span className="person-dot"><UserRound/></span><strong>{participant.full_name}</strong></div><span>{age===null?"—":`${age} anos`}</span><span>{participant.registrations.length}</span><b>Pendente</b></article>})}</div>:<div className="empty-state compact"><UserRound/><h2>Nenhum participante encontrado</h2><p>Cadastre manualmente ou use a importação CSV na próxima etapa.</p></div>}{open&&<div className="modal-wrap"><button className="backdrop" onClick={()=>setOpen(false)} aria-label="Fechar"/><form className="modal category-modal" onSubmit={submit}><button type="button" className="modal-x" onClick={()=>setOpen(false)}><X/></button><span className="eyebrow">CADASTRO MANUAL</span><h2>Novo participante</h2><div className="form-grid"><label className="wide">Nome completo<input name="full_name" required/></label><label>Data de nascimento<input name="birth_date" type="date"/></label><label>Sexo<select name="sex"><option value="">Não informado</option><option>Masculino</option><option>Feminino</option></select></label><label>Telefone<input name="phone"/></label><label>Responsável<input name="guardian"/></label><label>Polo<input name="school"/></label><label>Professor<input name="teacher"/></label><label>Graduação<input name="graduation"/></label><label>Evento<select value={eventId} onChange={(e)=>setEventId(e.target.value)}>{initialEvents.map((item)=><option value={item.id} key={item.id}>{item.name}</option>)}</select></label><label className="wide">Categoria<select name="category_id" required><option value="">Selecione...</option>{categories.map((category)=><option value={category.id} key={category.id}>{category.competition} · {category.name}</option>)}</select></label><label className="wide">Observações<input name="notes"/></label></div>{error&&<div className="form-error">{error}</div>}<div className="modal-actions"><button type="button" className="secondary" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary" disabled={loading}>{loading?"Salvando...":"Cadastrar e inscrever"}</button></div></form></div>}</section>}
