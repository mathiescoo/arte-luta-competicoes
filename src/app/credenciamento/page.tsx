import Link from "next/link";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import CheckinManager from "./checkin-manager";
export const dynamic="force-dynamic";
export default async function CheckinPage(){const supabase=await createClient();const {data:claims}=await supabase.auth.getClaims();const userId=claims?.claims?.sub;if(!userId)redirect("/entrar");const {data:role}=await supabase.from("user_roles").select("organization_id").eq("user_id",userId).limit(1).maybeSingle();if(!role)redirect("/painel");const {data:events}=await supabase.from("events").select("id,name").eq("organization_id",role.organization_id).neq("status","archived").order("created_at",{ascending:false});const eventIds=(events||[]).map((event)=>event.id);const {data:registrations}=eventIds.length?await supabase.from("registrations").select("id,event_id,attendance_confirmed,payment_confirmed,participants(full_name)").in("event_id",eventIds):{data:[]};return <main className="management-page"><Link href="/participantes" className="back-link">← Voltar para participantes</Link><div className="management-top"><div><span className="eyebrow">OPERAÇÃO</span><h1>Credenciamento</h1><p>Confirme pagamentos e presença na chegada do participante.</p></div></div><CheckinManager initialEvents={events||[]} initialRegistrations={registrations||[]}/></main>}
