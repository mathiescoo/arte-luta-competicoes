"use client";
import Link from "next/link";
import { Suspense, useState } from "react";
import { useSearchParams } from "next/navigation";
import { ArrowRight, LockKeyhole } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
function Login(){const [loading,setLoading]=useState(false);const [error,setError]=useState("");const params=useSearchParams();async function login(){setLoading(true);setError("");const {error:oauthError}=await createClient().auth.signInWithOAuth({provider:"google",options:{redirectTo:`${window.location.origin}/auth/callback?next=/painel`}});if(oauthError){setError(oauthError.message);setLoading(false)}}const accessError=params.get("erro");return <main className="login-page"><section className="login-brand"><div className="brand-mark">AL</div><span>ARTE-LUTA BRASIL</span><h1>Competições que<br/>mantêm a roda viva.</h1><p>Gestão segura de eventos, avaliações e resultados em tempo real.</p></section><section className="login-panel"><div className="login-icon"><LockKeyhole/></div><span className="eyebrow">ACESSO RESTRITO</span><h2>Entre na plataforma</h2><p>Use sua conta Google autorizada pela organização.</p><button className="google-button" onClick={login} disabled={loading}><span>G</span>{loading?"Abrindo Google...":"Continuar com Google"}<ArrowRight/></button>{(error||accessError)&&<div className="login-error">{error||"O Google não concluiu o acesso. Tente novamente."}</div>}<Link className="judge-signup-link" href="/cadastro-juiz">Sou juiz e quero solicitar acesso</Link></section></main>}
export default function LoginPage(){return <Suspense fallback={null}><Login/></Suspense>}
