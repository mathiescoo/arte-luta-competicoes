"use client";
import { useState } from "react";
import { Settings2, X } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
export default function EventSettings({eventId,initialSettings}:{eventId:string;initialSettings:Record<string,unknown>}){const [open,setOpen]=useState(false);const [requirePayment,setRequirePayment]=useState(initialSettings.require_payment===true);const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function save(){setLoading(true);setError("");const {error:updateError}=await createClient().from("events").update({settings:{...initialSettings,require_payment:requirePayment}}).eq("id",eventId);if(updateError){setError(updateError.message);setLoading(false);return}setOpen(false);setLoading(false)}return <><button className="secondary" onClick={()=>setOpen(true)}><Settings2/>Configurações</button>{open&&<div className="modal-wrap"><button className="backdrop" onClick={()=>setOpen(false)} aria-label="Fechar"/><div className="modal"><button className="modal-x" onClick={()=>setOpen(false)}><X/></button><span className="eyebrow">CONFIGURAÇÕES DO EVENTO</span><h2>Credenciamento</h2><label className="toggle-setting"><input type="checkbox" checked={requirePayment} onChange={(event)=>setRequirePayment(event.target.checked)}/><span><b>Exigir confirmação de pagamento</b><small>Quando desligado, o check-in mostra somente a presença dos participantes.</small></span></label>{error&&<div className="form-error">{error}</div>}<div className="modal-actions"><button className="secondary" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary" disabled={loading} onClick={save}>{loading?"Salvando...":"Salvar"}</button></div></div></div>}</>}
